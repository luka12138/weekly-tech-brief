# 跨任务规则核对记录

## 当前决定索引

2026-09-09最新决定（优先于下方历史记录）：周报不是信息全集或零遗漏审计，而是每周一提供AI公司近况的核证报告。每家公司0-5条最重要的独立事项，不凑数；先有限筛选，再核入选事实，完成即停止。取消全业务逐格覆盖、强制三轮检索、全部旧候选/活跃事项清零以及所有重大事项都须写入正文的发布前提。官方渠道保留，独立重要性校准兼看监管；未入选资料保留真实状态和理由，不假称已核。入选证据标准、排他截止、已知矛盾不得误导、历史版本隔离不变。头条最多12条，只提炼已入选事项；不得通过打包或附录藏入第六条。唯一执行契约为research_coverage.md。

以下为历史裁决，涉及范围和数量时已被上述最新决定替代，不再驱动全量重搜。

2026-09-09用户随后明确选择“允许线索隔离，重大事实缺口仍阻断”：未独立确认、尚未建立重大事件事实的线索可在第7节待核附录隔离，不计入当周事实/判断/图谱或回测；已确认重大事件关键事实缺口仍阻断。按research_coverage隔离契约执行，其余覆盖、来源、时点与验证标准不变。此决定替代旧“所有重大性未知线索都必须闭合才发布”的处理，不授权掩盖已知重大问题。

2026-09-09维护：下文保留历史逐次裁决，不将历史表格当现行数值。现行头条最多12件、不凑数；取消150/220行、单公司4条及24,000字符硬上限，按重大事实增篇；12公司官方渠道、方案二、独立监管/异源反查/活跃事项及完整证据均保留。日期未知时区按完整50小时规则，仍只由当前模型顺序执行和原单一任务调度。细则分别以时点、覆盖和上下文规范为准。

用户2026-09-08要求“将所有发现的问题，找到问题共性后落到skill中”，2026-09-09继续。此次将已发现故障归成skill九项执行规则，案例与机器/语义边界见手册“共性故障与验收”，详细契约保留唯一维护位置，不把每个案件变成永久特例。更新规则不等于9/7草稿已完成、旧失败已解决或整期token下降；不改变既有权限，不新建任务/定时器，不重写冻结实验。

## 日期区间固定规则

2026-09-08用户在“跑完剩余64个主候选后替换正式周报”的续跑中明确答复：“允许用完整保守时间区间判断，并固定到规则”。原文有公开日期、未知时区时，按UTC-12至UTC+14保留完整50小时可能区间；`news`整段须在本周，其余事实整段须早于截止，且所用正文版本必须可定位。跨周边界、无日期、未核版本不放行，不伪造精确时点。此决定取代旧“必须补出时区”对周中日期的机械阻断，不改变事实标准、重大性或全业务覆盖；详细契约只维护于时点规范，skill、入口、模板、手册、校验器和原自动化同步。维护与研究分阶段计量，冻结实验不追改。

核对日期：2026-09-07。只合并本项目规则，不复制完整对话、私人账号资料或其他项目指令。

## 来源筛选前提

2026-09-08用户先确认各家公司官方渠道必须保留；07:07:51Z的选择回复随后明确采用“方案二（推荐）：整理来源入口，并按事件去重、复用已核证据，只补缺失事实”。当前决定为方案二，不再待选；官方渠道、12公司全部业务、独立监管、在办事项复查、异源反查和证据标准全部保留。本次按当前模型顺序执行，不启用子代理，不改模型/任务或建设方案三的自动采集系统。规则唯一正文在 `research_coverage.md`，入口表在 `research_source_routes.md`，候选仍在原coverage账。旧失败记录和冻结实验不重写，维护计入完整总账；落地与测试通过不等于已证明整期token减少。

## 来源范围

- T1「小修小改」：`LOCAL_TASK_ID`；读取全部 40 个历史回合的用户要求，并核对被用户明确采纳的流程建议。
- T2「主对话」：`LOCAL_TASK_ID`；读取全部 24 个历史回合的用户要求，另读 2026-08-25 被明确采纳的“四个问题、六条建议”原文。
- T3「每周科技巨头晨间简报」：`LOCAL_TASK_ID`；当前对话要求全面核对未来数据、将规则固化到 skill/文档，并核对三个任务的规则说明。

核对原则：用户明确指令优先于助手自行建议；同一事项采用较新的明确要求；仍有实质冲突时询问用户，不擅自确定；一次性任务授权不升级为永久授权；推导出的工程约束须写明边界，不伪称用户逐字要求。

## 已合并规则

| 主题 | 来源 | 当前决定 | 落点 |
| --- | --- | --- | --- |
| 周期与定时 | T1 06-24，T2 初始自动化 | 每周一 09:00，上海时区，上一完整自然周；09:00 是触发时间，不是生成完成保证 | skill、时点规范、自动化 |
| 公司范围 | T1 初始 10 家；T2 08-14 加 OpenAI/Anthropic | 现行 12 家；保留早期真实 10 家历史范围 | skill、模板、校验器 |
| 头条数量 | T2 08-14 改为 10；T3 09-07不凑数，09-08固定12件 | 现行最多12件，按实际数量发布；零件明确无可确认重大事件，历史10件决定已被替代 | skill、规范、模板、数量校验、迁移脚本 |
| 联网及来源 | T2 初始；T1 06-30 自动查验来源 | 先原始/官方和监管，再权威媒体；至少一直接来源，重大/争议/供应关系尽量双源；传闻明确归因 | skill、手册、来源脚本 |
| 大型事件与去重 | T2 初始研究流程 | 筛重大经营/技术/资本/监管/产能变化；多篇报道合并，不用小新闻和无催化股价变动凑数 | skill |
| 中文可用性 | T1 06-30 指出英文过多、缺手册 | 读者界面、图例、解释中文优先；品牌、URL、代码/字段保留必要英文；提供可执行手册 | skill、README、手册 |
| 核心阅读路径 | T2 08-25 明确采纳六建议 | 头条、投资判断、趋势、催化在前；关系/图谱放附录；只展开变化公司 | skill、模板 |
| 投资研究而非新闻堆积 | T2 08-25 | 每事件写影响指标、预期差、验证条件；无当时一致预期不虚构“超预期” | skill、模板、时点规范 |
| 具体产品供应关系 | T1 06-24 | 稳定 Edge ID、明确方向/产品/服务/证据，详细区分与上周变化；基线不足明示 | skill、手册 |
| 年度主营产品图 | T1 06-30 | 覆盖各公司所有主营产品上下游，以截至当时可得的年度官方证据为优先，非今天最新年度材料回填 | skill、时点规范 |
| Obsidian 图风格 | T1 06-30；T2 08-24 使用 obsidian-skills | 两类网络图，使用现行 JSON Canvas 1.0 与同源 SVG，可读、无重叠、证据分级 | skill、手册、既有图脚本 |
| 取消重复表示层 | T2 08-25 采纳减重改造 | JSON 为事实源，Canvas/SVG 为显示层，不恢复 Mermaid，不复制完整关系 Markdown 表 | skill、模板 |
| 图表更新频率 | 早期每周重画；T2 08-25 改为按变化/季度 | 供应图实质变化才生成；产品图季度首周一或实质变化才生成；其他沿用 | skill、图表计划 |
| 供应证据分层 | T1 06-29 采纳改进 | 已确认、媒体、长期基线、待确认分清；无当周直接证据不能 new/strengthened；弱证据不画成官方确认 | skill、手册、图表策略 |
| 固定闸门 | T1 06-29/06-30 改造；07-07 要求使用 | 单入口 `run_quality_gate.py`；来源日志绑定当前输入哈希和 URL；增加时点证据阻断 | skill、手册、脚本 |
| 发布真实性 | T1 多次核对推送；T2 08-11 查遗漏 | 实际通过才称通过，实际推送才称同步；离线/认证/失败交付明确状态；查连续性不补造报告 | skill、规范、自动化 |
| 跨设备部署 | T1 06-24 Windows 生成、Mac 查看 | 保留为目标架构；GitHub 同步，避免两个写入调度；未经当前授权不迁移主机或新建同名任务 | skill、README |
| 公开仓库 | T1 06-24 私有，06-29 改公开 | 公开为较新项目意图，防止凭据/私人材料入库；此记录不是实时 GitHub 权限审计 | skill、README |
| 未来数据审计 | T3 09-07 | 断言首次公开、上海截止、页面段落版本、财务口径、报告版本时点分开查；未知不放行 | 时点规范、测试 |
| 规则持久化 | T3 09-07 两次明确要求 | skill 管决策入口，专门文档管细则，记录冲突取舍；自动化必须先读，不能仅靠本次聊天 | skill、AGENTS、全部入口 |
| 冲突处理 | T3 09-07 允许询问 | 未明确被替代的实质冲突先问；本次已询问“固定十条/不凑数”并按回复更新 | skill、本记录 |
| 本次历史替换 | T3 09-07 指定三项问题，随后明确“替换掉对应原有旧周报” | 原路径替换 7/6、7/27 正文；九期事后迁移版登记真实修订时间并排除原周一回测，其余七期仅补标识。先独立归档，不抹除 Git 原版；本次授权不等于自动提交推送或今后静默覆盖 | 历史版本记录、版本校验器、两期 correction_evidence、skill |
| 单一任务执行 | T3 09-07 明确要求合并三个任务，只保留一个定期执行任务 | 保留“每周科技巨头晨间简报”；另外两个归档而非删除。原 `automation` 原地改为绑定保留任务的定时执行，周一上海 09:00 不变；模型跟随保留任务，不再有独立运行配置。成果和未完成事项写入交接记录，不复活旧规则，也不把未完成说成完成 | task_consolidation、AGENTS、skill、手册、README、定时配置和运行记忆 |

## 上下文减重补充

2026-09-07 补充裁决：用户要求优化 skill 的缓存输入，前提是不损失精度与真实性。采用同任务按需装载、完整索引与哈希绑定展开、具体版本证据复用、短工具输出和阶段接续；详细流程见 `context_efficiency.md`。不减少公司/产品研究或核验，不改模型/推理设置，不禁用缓存，不新建任务，不设置压倒真实性的 token 上限。局部输出减重与整期实际消耗分开计量，未实测不承诺整期降幅。此段是新要求及其工程落实，不复活旧的精简证据要求。

## 不沿用的旧要求

2026-09-07用户确认执行层改造：正文格式先预检；工具返回前完成类型检查和有效内容提取；减少重复输出与无效往返；例行生产、额外补证和维护分阶段计量且保留完整总账。目标是减少不必要上下文，而非降低缓存命中率。通过 `validate_weekly_brief.py --preflight-only`、主闸门前置检查、`tool_output.cjs` 和 `measure_run_tokens.py` 落实；操作细则唯一维护在 `context_efficiency.md` 及其工具契约。不改研究覆盖或证据标准，不重写冻结的9/7实验，也不以阶段分账隐藏失败成本。

2026-09-07进一步确认：用户答复“是，固定缓存优化版；监管防漏修改单独评估”。因此本次9/7重跑使用快照 `archives/2026-09-07_skill_before_regulatory_feedback.tar.gz`；后加 `research_coverage.md` 的全主营业务发现、独立监管检索及成稿前反查只用于后续执行，不能把其假定收益计入缓存优化。新规则不改变最多十件、不凑数和时点底线。实测总token未降低，见本期重跑评估，不宣称缓存优化已验收。旧日期解析和来源分类修复为执行兼容修复，不是监管发现流程已经生效。

2026-09-08用户在重跑中要求“为什么前前后后会有这么多遗漏，到底skill哪里有漏洞，尽快整改”。暂停未完成发布，先修复发现流程：批量结果按实际公司/业务验收、每批候选即时落JSON、非财务重大性与法律中间阶段、活跃事项续查、异源反查和无变化结论闭环；在主闸门强制检查覆盖记录。详见research_coverage；不设新闻配额、不降低时点/证据标准、不自动放宽篇幅、不改模型或增加任务。结构测试不能认证零遗漏，原冻结实测不重算；本轮中途改变skill，后续若继续重跑须披露混合版本和维护成本，不能当纯缓存策略A/B。

| 历史事项 | 不沿用的原因 |
| --- | --- |
| 财新会员 V2、Safari 登录接入（T2 07-31 上午） | 同日 11:27 用户明确取消上午所有新增工作，恢复原流程；不能恢复会员依赖、自动抓取登录态或请求账号 |
| 5-8 头条、恰好 10 头条、现行只覆盖 10 家 | 公司范围按 08-14 扩为 12；头条上限按 09-07 “不凑数”最新确认，不是删改早期历史对象的授权 |
| 每家公司重复空小节、Mermaid 与完整关系表、每周重建年度图 | 被 08-25 已采纳的精简方案替代 |
| “重建所有过往报告/图片并替换 GitHub 老版”（T2 08-24/08-26） | 当次重建范围；不允许今后每次审计静默覆盖历史。现在修订还须遵守版本可得性规范 |
| “删除旧结果”“清理 supply_graph_baseline 2.json” | 针对当次结果或特定文件，不授权自动删除今后所有带 `2` 的用户文件 |
| 多次请求 gpt-5.5 high / 新 agent 复查（T1） | 保留重要改造应独立复核的偏好；指定模型与派遣属于当次要求，不强制每周/每个回答生成子代理，不修改主自动化模型 |
| 把其他 GitHub 仓库设私密、PAT 查询和截图报错 | 属于当次账号/环境操作，不是周报 skill 的行为权限，不传播账号信息 |

## 本次新增的可执行约束

2026-09-08在9/7重跑中，用户先答复“允许按事实增加篇幅，头条改为12件”，随后明确“这次授权不是本期例外，而是加入skill中的固定规则”。此前助手误读成单期例外，现已撤销该配置，固定采用头条最多12件、按事实增加篇幅、取消150/220行上限，适用于本次和以后；仍不凑数，保留八字段、核心字符保护与完整证据检查，不重写历史冻结实验。skill、模板、手册、AGENTS、README、校验器与原自动化同步。误读及修改返工计入维护和完整总账，不冒称完全未变skill的A/B。

时点规范和逐条证据契约是为落实 T3 而制定的工程规则，不声称是旧对话已有的机器能力：2026-09-14 起新报告缺失证据即阻断；更早报告未补证据标为 `legacy_unverified`。这是前向生效，不把历史未核验报告偷偷改成通过。

数字语义、断言是否完整拆分、产品图逐边可得性仍需要原文复核。机器校验只验证已记录日期/版本/哈希/目标的约束，不自动证明事实真实。

更改任一决定时，记录新的用户日期、旧规则、新规则及影响文件；同步检查入口，避免正文、skill、模板、自动化各自维护冲突版本。

2026-09-08续跑发现上次同步仍有遗漏：时点规范保留了十件上限，校验器保留了单公司四件硬上限。本次改为固定12件头条、公司详情至少一件且按重大事实增加，八字段检查不变，并增加五件事件及缺字段反例测试。此前“全部同步”不准确，此次补修及失败重试计入维护总账。

同次续跑又从业务专属入口补出Xbox服务条件、Instagram账号披露政策及Microsoft财务重列。本轮也未充分落实原有全业务规则，不能全部归咎于旧模型。补充说明总新闻页、专属业务入口、IR/申报及状态事件的覆盖边界，并明确官方页自动AI摘要不能替代正文。未建立完整检查记录前保持draft；新要求不追溯计入原缓存优化实验。24000字符上限本次发生实际冲突，已询问用户，尚未得到答复时不擅自撤销。

继续核验再次补出英国ATT、Windows专属公告、Amazon物流预测及Meta诉讼/设备隐私执行，撤回4条过宽覆盖验收。同步增加批次合计载荷保护、先定位再扩读、反例撤回覆盖状态、当事人来源分类及推荐栏独立时点规则。这是对用户既有“减无效上下文、不减覆盖证据、暴露错误”要求的落实，不新增新闻配额或放宽证据。当前仍未验收成功，返工和不一致执行计入总账；24k字符问题继续待答，未将12件头条授权解释成放弃时点或来源标准。

2026-09-08后续确认：用户答复“取消24,000字符硬上限，固定按重大事实需要增加篇幅？”，据此永久取消24,000字符硬上限，与已取消的150/220行上限一并适用于本次重跑及以后；不另设替代篇幅硬上限，也无需逐期申请扩篇。头条仍最多12件、不凑数；先合并重复表达，保留全部重大事实、八字段、口径、限制和完整证据，不降低研究覆盖或时点标准。此决定明确取代上文“保留核心字符保护”和“待答”，不改写原失败记录或冻结实验。同步范围为skill、AGENTS、README、规范、模板、手册、覆盖/上下文文档、校验器、测试及唯一原自动化；移除篇幅阻断不代表报告已通过事实闸门。
