# 周报项目执行入口

当前固定口径（2026-09-09最新授权）：每周一提供12家AI相关公司近况的核证报告，每家公司0-5条最重要的独立事项，不凑数、不追求零遗漏、不搜集全部信息。完成有限筛选和入选核验即收尾；旧全量覆盖/全部候选清零不再是发布前提。

生成、补跑、修订、审计周报或维护规则前，读取：

1. `.agents/skills/weekly-tech-brief/SKILL.md`
2. `docs/point_in_time_policy.md`，时点/事实规则的唯一维护正文
3. `docs/weekly_brief_template_v2.md`、`docs/verification_manual.md`
4. `docs/research_coverage.md`，保留各家官方渠道，有限筛选和一轮独立重要性校准；最多5条/公司，入选事实缺口及会误导结论的矛盾不得放行
5. `docs/task_consolidation.md`，唯一任务入口、共享成果和未完成事项

已采用来源优化方案二：官方入口见 `docs/research_source_routes.md`；候选工作卡仍在原coverage账，先看 `validate_research_coverage.py --worklist --report PATH`，按事件去重、复用已核具体版本、只补明确缺口。执行细则唯一维护在research_coverage；不把方案二扩大为自动采集系统，保留官方渠道，监管线索与独立校准按新的有限筛选范围执行。

读取策略见 skill 与 `docs/context_efficiency.md`：同轮未变规则不反复全文装载，基线先完整索引再按需展开，变更/相关/弱证据项必读；不减少研究和校验，摘要不替代原证据。不为省缓存输入新建任务或降低模型设置。

正文初稿/修改后先只读格式预检，再绑定证据、生成图和审查来源；工具返回前检查类型并提取完整有效语义块。生产、额外补证、维护分阶段计量，未归类和失败返工保留在完整总账。细则与工具见 `docs/context_efficiency.md`，目标是减少不必要上下文，不是降低缓存命中率。

本项目只在“每周科技巨头晨间简报”任务中定期执行与维护；原“主对话”“小修小改”只作归档追溯。不另建周报任务或重复定时器。

规则冲突先查 `docs/rule_decisions.md` 的跨任务核对记录，不复活已撤销要求。

按skill九项共性规则逐批推进：结果级覆盖、事件身份、原子事实、时间与版本、落盘回读、更正传播、阶段闸门、完整总账及规则版本。适用检查与机器边界见手册“共性故障与验收”；计数齐全、已写规则或局部测试通过都不等于研究/发布完成。

2026-09-09用户允许未确认线索按research_coverage的隔离契约进入第7节待核附录，不计入当周事实、投资判断或图谱；入选重大事实的关键缺口仍阻断相关断言；其余未入选线索可留资料池，不要求逐条附录。

仍有实质冲突时询问用户，不擅自固化取舍。最新固定规则：每家公司最多5条，头条最多12件且仅提炼入选事项，不凑数；总行数/字符不设硬上限，但不无限增加事件。保留入选事项八字段、口径和限制。

只使用报告周一上海 00:00 前已公开的具体版本；未知公开时间不能判通过。核对财务期间和单位，区分旧闻、计划与结果，保存原始历史版本。

日期明确但时区未知时，按时点规范使用UTC-12至UTC+14的完整可能区间，不伪造精确时间；只有整个区间落在允许范围且具体正文版本已核才纳入。跨周边界、无日期或版本未知仍阻断。

2026-09-14 起新周报必须有逐条 `logs/YYYY-MM-DD_temporal_evidence.json`。先用 `scripts/validate_point_in_time.py --inventory` 列目标并实际核验，再运行 `scripts/run_quality_gate.py`。不能为过闸门编造证据；`legacy_unverified` 不是历史回测认证。

审计不自动覆盖旧报告或提交推送。保护无关文件、账号和凭据。规则修改应同步检查 skill、规范、模板、手册和自动化入口，避免只在对话中临时修补。
